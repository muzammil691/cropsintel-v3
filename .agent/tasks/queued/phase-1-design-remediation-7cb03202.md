# Task: Design remediation — 7cb03202

**Auto-generated by Atlas conductor** after Designer audit failed on commit 7cb0320208dffea2d97c02bd5153508531da8cf4.

**Original commit:** feat(atlas-9A): Settings shell + Connections vault read/test/delete (no Add yet)

## Gaps to fix

1. **[low] motion** — src/components/atlas/ConnectionCard.tsx:186 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas/ConnectionCard.tsx:186

2. **[low] motion** — src/pages/atlas/settings/AuditPage.tsx:109 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/pages/atlas/settings/AuditPage.tsx:109

3. **[low] motion** — src/pages/atlas/settings/ConnectionsPage.tsx:82 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/pages/atlas/settings/ConnectionsPage.tsx:82

4. **[medium] motion** — ConnectionCard.tsx line 186: hover: class without transition- — abrupt state change on interactive element
   Fix: Add transition-colors duration-200 to the hover element class list
   Where: src/components/atlas/ConnectionCard.tsx:186

5. **[medium] motion** — AuditPage.tsx line 109: hover: class without transition- — abrupt state change on Button refresh action
   Fix: Verify Button component has transition-colors duration-200, or add explicit className override
   Where: src/pages/atlas/settings/AuditPage.tsx:109

6. **[medium] motion** — ConnectionsPage.tsx line 82: hover: class without transition- — abrupt state change on refresh button or cards
   Fix: Add transition-colors duration-200 to any hover: utilities on interactive elements
   Where: src/pages/atlas/settings/ConnectionsPage.tsx:82

7. **[low] states** — AuditPage.tsx line 105: <select> element missing focus-visible:ring-2 ring-emerald-600/50 — has focus:ring-2 focus:ring-emerald-500/40 instead (inconsistent with design system spec)
   Fix: Change focus:ring-2 focus:ring-emerald-500/40 to focus-visible:ring-2 ring-emerald-600/50 per design system
   Where: src/pages/atlas/settings/AuditPage.tsx:105

8. **[low] states** — SettingsLayout.tsx NavLink transitions use duration-150 instead of design system standard duration-200
   Fix: Change transition-colors duration-150 to transition-colors duration-200 on lines 66 and NavLink className builder
   Where: src/pages/atlas/settings/SettingsLayout.tsx:66

## Acceptance criteria

1. All gaps above resolved or explicitly waived in the commit message.
2. `npm run build` clean.
3. Re-run Designer audit — verdict must be `pass`.
