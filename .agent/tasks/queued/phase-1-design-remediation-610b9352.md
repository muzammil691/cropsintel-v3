# Task: Design remediation — 610b9352

**Auto-generated by Atlas conductor** after Designer audit failed on commit 610b9352629050cb8146fd8378c56e9f8da321db.

**Original commit:** feat: Position Reports UI with analytics layer (phase-1.7)

## Gaps to fix

1. **[medium] mobile-responsive** — src/App.tsx: uses shadcn components but has no responsive prefixes (sm:/md:/lg:)
   Fix: Add Tailwind responsive prefixes for layouts that should adapt across viewports
   Where: src/App.tsx

2. **[low] motion** — src/pages/PositionReports.tsx:337 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/pages/PositionReports.tsx:337

3. **[low] motion** — src/pages/PositionReports.tsx:436 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/pages/PositionReports.tsx:436

4. **[low] motion** — src/pages/PositionReports.tsx:482 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/pages/PositionReports.tsx:482

5. **[high] mobile-responsive** — PositionReports.tsx uses no responsive prefixes (sm:/md:/lg:) for layout, table, or filter controls—will overflow on mobile
   Fix: Add responsive classes: hide filters in Sheet on mobile, stack cards vertically, use responsive text sizes (text-sm sm:text-base), apply px-4 sm:px-6 lg:px-8 pattern
   Where: src/pages/PositionReports.tsx:27

6. **[medium] motion** — Lines 337, 436, 482 have hover: classes without transition-* causing abrupt state changes
   Fix: Add transition-colors duration-200 to all interactive elements with hover states
   Where: src/pages/PositionReports.tsx:337

7. **[high] accessibility** — Detail drawer implementation (selectedReport state line 49) is truncated but likely missing focus trap, Escape handler, and aria-modal per anti-patterns
   Fix: Use shadcn Sheet component with built-in focus trap instead of custom drawer, ensure onOpenChange handles Escape key
   Where: src/pages/PositionReports.tsx:49

8. **[medium] loading-states** — PositionReportsSkeleton component is referenced (line 289) but not shown in diff—verify it uses Skeleton primitive, not just empty divs
   Fix: Ensure PositionReportsSkeleton uses shadcn Skeleton component with proper spacing matching actual content layout
   Where: src/pages/PositionReports.tsx:289

9. **[medium] states** — Filter inputs (dateFrom/dateTo/marketFilter lines 36-38) likely missing focus-visible ring states and disabled states
   Fix: Ensure all Input components have focus-visible:ring-2 focus-visible:ring-emerald-600/50 and disabled:opacity-50 disabled:cursor-not-allowed
   Where: src/pages/PositionReports.tsx:36

10. **[high] accessibility** — Sort toggle buttons (toggleSort function line 263) likely implemented as clickable headers without proper button semantics or aria-sort
   Fix: Use Button variant='ghost' with aria-sort attribute for sortable column headers, add aria-label describing sort direction
   Where: src/pages/PositionReports.tsx:263

11. **[medium] mobile-responsive** — App.tsx migration banner (line 46-61) uses fixed positioning with max-w-md but no bottom-safe spacing or mobile-optimized touch target
   Fix: Add pb-safe or pb-4 to Alert, ensure dismiss button is min-h-[44px] min-w-[44px] for touch targets
   Where: src/App.tsx:46

## Acceptance criteria

1. All gaps above resolved or explicitly waived in the commit message.
2. `npm run build` clean.
3. Re-run Designer audit — verdict must be `pass`.
