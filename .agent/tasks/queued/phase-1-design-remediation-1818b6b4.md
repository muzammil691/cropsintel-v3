# Task: Design remediation — 1818b6b4

**Auto-generated by Atlas conductor** after Designer audit failed on commit 1818b6b45aaa546df89742c911928fc270319eeb.

**Original commit:** feat: session-5-verifier-dialog

## Gaps to fix

1. **[medium] mobile-responsive** — src/components/atlas-plan/VerifierDialogPopup.tsx: uses shadcn components but has no responsive prefixes (sm:/md:/lg:)
   Fix: Add Tailwind responsive prefixes for layouts that should adapt across viewports
   Where: src/components/atlas-plan/VerifierDialogPopup.tsx

2. **[low] motion** — src/components/atlas-plan/VerifierDialogPopup.tsx:134 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas-plan/VerifierDialogPopup.tsx:134

3. **[low] motion** — src/components/atlas-plan/VerifierDialogPopup.tsx:222 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas-plan/VerifierDialogPopup.tsx:222

4. **[low] motion** — src/components/atlas-plan/VerifierDialogPopup.tsx:233 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas-plan/VerifierDialogPopup.tsx:233

5. **[high] shadcn-usage** — VerifierDialogPopup.tsx implements a custom dialog with `role="dialog"` and raw div overlays instead of using shadcn Dialog component, which provides focus trap, ESC handling, and ARIA attributes out of the box
   Fix: Replace custom dialog implementation (lines 113-127) with shadcn Dialog/DialogContent/DialogHeader/DialogFooter components
   Where: src/components/atlas-plan/VerifierDialogPopup.tsx:113

6. **[high] accessibility** — Dialog lacks focus trap—users can tab out to background content while modal is open. shadcn Dialog provides this automatically via radix-ui
   Fix: Use shadcn Dialog component which implements proper focus management and portal rendering
   Where: src/components/atlas-plan/VerifierDialogPopup.tsx:113

7. **[medium] shadcn-usage** — Clickable overlay div (line 122 `onClick={(e) => { if (e.target === e.currentTarget) onClose?.() }`) is a raw div with click handler instead of using Dialog's onOpenChange
   Fix: Use shadcn Dialog with onOpenChange prop for proper overlay click handling
   Where: src/components/atlas-plan/VerifierDialogPopup.tsx:122

8. **[medium] motion** — Static analysis flagged lines 134, 222, 233 with hover: classes lacking transition-. Button hover states should have transition-colors duration-200
   Fix: Add transition-colors duration-200 to all Button components with hover states
   Where: src/components/atlas-plan/VerifierDialogPopup.tsx:134

9. **[high] mobile-responsive** — Modal has no responsive layout—fixed max-w-lg and p-4 padding work on mobile, but no sm:/md: prefixes for typography, spacing, or layout adjustments. Chat collapse logic exists but dialog itself has no mobile adaptation
   Fix: Add responsive prefixes: sm:max-w-xl md:max-w-2xl, adjust padding sm:p-6, and consider full-screen on mobile with sm:rounded-none sm:h-full
   Where: src/components/atlas-plan/VerifierDialogPopup.tsx:126

10. **[medium] accessibility** — Textarea for abort reason (line ~100-110 inferred from logic) needs paired Label with htmlFor. Current implementation likely missing this
   Fix: Add <Label htmlFor="abort-reason">Reason for abort</Label> above Textarea with id="abort-reason"
   Where: src/components/atlas-plan/VerifierDialogPopup.tsx

11. **[low] states** — Buttons during busy state should show disabled styling (opacity-50 cursor-not-allowed), but implementation unclear if this is applied to non-busy buttons when one is active
   Fix: Ensure all Buttons have disabled={busy !== null} to prevent double-clicks and show proper disabled state
   Where: src/components/atlas-plan/VerifierDialogPopup.tsx

## Acceptance criteria

1. All gaps above resolved or explicitly waived in the commit message.
2. `npm run build` clean.
3. Re-run Designer audit — verdict must be `pass`.
