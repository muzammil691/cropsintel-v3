# Task: Design remediation — 56908d04

**Auto-generated by Atlas conductor** after Designer audit failed on commit 56908d04a4dbcd3abe5c00bedf8a9be7dfef0ac0.

**Original commit:** fix: phase-1-design-remediation-9030b33c (designer audit gaps)

## Gaps to fix

1. **[high] typography** — ConceptsPanel.tsx still uses text-[11px] on four buttons (Paste, Upload, Voice, Past chat). Design system mandates text-xs as the minimum scale — arbitrary [11px] is not a token.
   Fix: Replace className="text-[11px] h-7" with className="text-xs h-7" on lines 187, 199, 210, 219
   Where: src/components/atlas-plan/ConceptsPanel.tsx:187

2. **[medium] states** — PlanActionButtons.tsx size="icon-xs" buttons (lines 36-64) have aria-labels but no explicit focus-visible:ring-2 ring-emerald-600/50 class. Design system requires focus-visible on all interactive elements.
   Fix: Add focus-visible:ring-2 focus-visible:ring-emerald-600/50 to all four Button components in PlanActionButtons
   Where: src/components/atlas-plan/PlanActionButtons.tsx:36

3. **[medium] accessibility** — ConceptsPanel.tsx line 236: file input has aria-hidden="true" and tabIndex={-1}, which is correct for a visually-hidden input triggered by a visible button. However, verify the visible Upload button (line 197) properly triggers fileInputRef.current?.click() — diff truncates the handler implementation.
   Fix: Audit that the Upload button onClick at line 197 correctly invokes fileInputRef.current?.click() to maintain keyboard accessibility for file uploads
   Where: src/components/atlas-plan/ConceptsPanel.tsx:197

4. **[low] spacing** — PlanTree.tsx line 168: gap-1 on mobile (sm:gap-2) is valid but inconsistent with the default gap-4 mentioned in design system. Consider gap-2 base and sm:gap-3 for better visual rhythm if this is default spacing.
   Fix: Review if gap-1 is intentionally compact; if so, document exception. Otherwise use gap-2 base per design system compact spacing.
   Where: src/components/atlas-plan/PlanTree.tsx:168

## Acceptance criteria

1. All gaps above resolved or explicitly waived in the commit message.
2. `npm run build` clean.
3. Re-run Designer audit — verdict must be `pass`.
