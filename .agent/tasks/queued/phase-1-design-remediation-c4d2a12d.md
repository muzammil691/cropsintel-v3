# Task: Design remediation — c4d2a12d

**Auto-generated by Atlas conductor** after Designer audit failed on commit c4d2a12d0b3f48354631f7c11ca57444301f7f2a.

**Original commit:** fix(atlas-cockpit): repair Concepts delete + Workshop multi-select + instructional copy

## Gaps to fix

1. **[medium] accessibility** — <input> without id or aria-label — likely missing <Label> association
   Fix: Pair <input id="x"> with <Label htmlFor="x"> or add aria-label
   Where: src/components/atlas-plan/ConceptsPanel.tsx:11

2. **[low] motion** — src/components/atlas-plan/ConceptsPanel.tsx:971 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas-plan/ConceptsPanel.tsx:971

3. **[low] motion** — src/components/atlas-plan/PlanWorkshop.tsx:668 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas-plan/PlanWorkshop.tsx:668

4. **[high] accessibility** — DeleteConfirmDialog Dialog component at line 945 has onOpenChange that only fires onCancel when !busy, but does not properly handle escape-key dismissal or click-outside during loading states. The design system requires modals to have proper focus trap, but no focus management code is present.
   Fix: Add radix-ui Dialog's built-in focus trap (verify DialogContent uses it), and ensure onOpenChange properly handles all dismissal paths. Add onEscapeKeyDown and onInteractOutside handlers that respect the busy state.
   Where: src/components/atlas-plan/ConceptsPanel.tsx:945

5. **[high] states** — The destructive confirm Button at line 971 does not have a disabled={busy} prop, allowing users to double-click and potentially trigger duplicate delete requests. The busy state is only reflected in the loading spinner icon.
   Fix: Add disabled={busy} prop to the Button component: <Button variant="destructive" disabled={busy} onClick={onConfirm}>
   Where: src/components/atlas-plan/ConceptsPanel.tsx:971

6. **[medium] motion** — Button at line 971 uses hover:bg-rose-700 without a transition class (flagged by static linter). This creates an abrupt state change that violates the design system's motion principle requiring transition-colors duration-200 on interactive elements.
   Fix: Add transition-colors duration-200 to the Button className: className="transition-colors duration-200"
   Where: src/components/atlas-plan/ConceptsPanel.tsx:971

7. **[medium] accessibility** — Static checks flagged an <input> without id or aria-label, likely the fileInputRef or folderInputRef at lines 218-219. These hidden file inputs need proper labeling for screen readers even though they're triggered programmatically.
   Fix: Add aria-label to both file input refs: <input ref={fileInputRef} type="file" aria-label="Upload concept file" className="sr-only" /> and similar for folderInputRef with aria-label="Upload concept folder"
   Where: src/components/atlas-plan/ConceptsPanel.tsx:218

8. **[low] loading-states** — The confirm button at line 971 shows a Loader2 icon during busy state but does not disable pointer events or show cursor-not-allowed, creating confusion about interactivity.
   Fix: Ensure the Button's disabled state automatically applies opacity-50 cursor-not-allowed (shadcn default), or add explicit className when busy: className={cn('transition-colors duration-200', busy && 'cursor-not-allowed')}
   Where: src/components/atlas-plan/ConceptsPanel.tsx:971

## Acceptance criteria

1. All gaps above resolved or explicitly waived in the commit message.
2. `npm run build` clean.
3. Re-run Designer audit — verdict must be `pass`.
