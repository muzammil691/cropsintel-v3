# Task: Design remediation — df5e1cc7

**Auto-generated by Atlas conductor** after Designer audit failed on commit df5e1cc762d051ea0a6b5a29cda630fbd16c9fdd.

**Original commit:** feat: session-7-workshop-fix-and-folder-concepts

## Gaps to fix

1. **[medium] accessibility** — <input> without id or aria-label — likely missing <Label> association
   Fix: Pair <input id="x"> with <Label htmlFor="x"> or add aria-label
   Where: src/components/atlas-plan/ConceptsPanel.tsx:11

2. **[medium] accessibility** — <input> without id or aria-label — likely missing <Label> association
   Fix: Pair <input id="x"> with <Label htmlFor="x"> or add aria-label
   Where: src/components/atlas-plan/ConceptsPanel.tsx:633

3. **[medium] accessibility** — <input> without id or aria-label — likely missing <Label> association
   Fix: Pair <input id="x"> with <Label htmlFor="x"> or add aria-label
   Where: src/components/atlas-plan/ConceptsPanel.tsx:637

4. **[medium] accessibility** — <input> without id or aria-label — likely missing <Label> association
   Fix: Pair <input id="x"> with <Label htmlFor="x"> or add aria-label
   Where: src/components/atlas-plan/ConceptsPanel.tsx:648

5. **[medium] accessibility** — <input> without id or aria-label — likely missing <Label> association
   Fix: Pair <input id="x"> with <Label htmlFor="x"> or add aria-label
   Where: src/components/atlas-plan/ConceptsPanel.tsx:666

6. **[medium] accessibility** — <input> without id or aria-label — likely missing <Label> association
   Fix: Pair <input id="x"> with <Label htmlFor="x"> or add aria-label
   Where: src/components/atlas-plan/ConceptsPanel.tsx:677

7. **[low] motion** — src/components/atlas-plan/ConceptsPanel.tsx:560 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas-plan/ConceptsPanel.tsx:560

8. **[low] motion** — src/components/atlas-plan/ConceptsPanel.tsx:641 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas-plan/ConceptsPanel.tsx:641

9. **[low] motion** — src/components/atlas-plan/ConceptsPanel.tsx:658 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas-plan/ConceptsPanel.tsx:658

10. **[low] motion** — src/components/atlas-plan/ConceptsPanel.tsx:670 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas-plan/ConceptsPanel.tsx:670

11. **[low] motion** — src/components/atlas-plan/ConceptsPanel.tsx:809 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas-plan/ConceptsPanel.tsx:809

12. **[low] motion** — src/components/atlas-plan/ConceptsPanel.tsx:810 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas-plan/ConceptsPanel.tsx:810

13. **[low] motion** — src/components/atlas-plan/ConceptsPanel.tsx:899 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas-plan/ConceptsPanel.tsx:899

14. **[low] motion** — src/components/atlas-plan/ConceptsPanel.tsx:970 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas-plan/ConceptsPanel.tsx:970

15. **[low] motion** — src/components/atlas-plan/WorkshopErrorBoundary.tsx:152 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas-plan/WorkshopErrorBoundary.tsx:152

16. **[high] accessibility** — Multiple <input> elements without associated <Label> or aria-label. Static checks flagged 6 instances. Visible in intake forms (paste/upload modes), edit dialog, and filter input in ConceptsPanel.
   Fix: Wrap all input fields with <Label htmlFor={id}> from shadcn/ui or add explicit aria-label attributes. Example: filter input needs <Label htmlFor="concept-filter">Filter concepts</Label>
   Where: src/components/atlas-plan/ConceptsPanel.tsx

17. **[medium] motion** — 9 hover: classes without transition- causing abrupt state changes. Lines 560, 641, 658, 670, 809, 810, 899, 970 in ConceptsPanel and line 152 in WorkshopErrorBoundary.
   Fix: Add transition-colors duration-200 to all elements with hover: states. Example: className="hover:bg-slate-100 transition-colors duration-200"
   Where: src/components/atlas-plan/ConceptsPanel.tsx:560

18. **[high] accessibility** — Custom interactive elements (folder collapse chevrons, action menu triggers) likely missing keyboard handlers and focus-visible states. ChevronDown/ChevronRight buttons in folder rows need Enter/Space handlers.
   Fix: Add onKeyDown handlers for Enter/Space on custom clickable elements, ensure focus-visible:ring-2 ring-emerald-600/50 on all interactive components, or replace with shadcn Button component
   Where: src/components/atlas-plan/ConceptsPanel.tsx

19. **[medium] states** — Interactive icon buttons (MoreHorizontal action menu trigger, folder chevrons) lack explicit disabled, hover, and focus-visible state declarations per design system requirements.
   Fix: Ensure all interactive elements have explicit :hover, :focus-visible (ring-2 ring-emerald-600/50), :disabled (opacity-50 cursor-not-allowed), and :active states defined
   Where: src/components/atlas-plan/ConceptsPanel.tsx

20. **[medium] motion** — WorkshopErrorBoundary reload button at line 152 has hover: class without transition
   Fix: Add transition-colors duration-200 to the Button or parent element with hover state
   Where: src/components/atlas-plan/WorkshopErrorBoundary.tsx:152

21. **[medium] accessibility** — Folder upload input with webkitdirectory attribute likely missing descriptive label. Standard file inputs need clear label text explaining the folder selection capability.
   Fix: Add <Label htmlFor="folder-upload-input">Upload folder (will process text files only)</Label> paired with id="folder-upload-input" on the input element
   Where: src/components/atlas-plan/ConceptsPanel.tsx

## Acceptance criteria

1. All gaps above resolved or explicitly waived in the commit message.
2. `npm run build` clean.
3. Re-run Designer audit — verdict must be `pass`.
