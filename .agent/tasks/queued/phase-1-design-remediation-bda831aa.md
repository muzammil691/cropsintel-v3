# Task: Design remediation — bda831aa

**Auto-generated by Atlas conductor** after Designer audit failed on commit bda831aad0c5a5984fffcfc68f95dac7c0e43b84.

**Original commit:** feat: AI agent landing scaffold + 10-deep gate (phase-1.3b)

## Gaps to fix

1. **[medium] mobile-responsive** — src/components/landing/UpgradePitchInline.tsx: uses shadcn components but has no responsive prefixes (sm:/md:/lg:)
   Fix: Add Tailwind responsive prefixes for layouts that should adapt across viewports
   Where: src/components/landing/UpgradePitchInline.tsx

2. **[medium] mobile-responsive** — src/components/landing/UpgradeToVerifiedInline.tsx: uses shadcn components but has no responsive prefixes (sm:/md:/lg:)
   Fix: Add Tailwind responsive prefixes for layouts that should adapt across viewports
   Where: src/components/landing/UpgradeToVerifiedInline.tsx

3. **[low] motion** — src/pages/Landing.tsx:111 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/pages/Landing.tsx:111

4. **[low] motion** — src/pages/Landing.tsx:132 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/pages/Landing.tsx:132

5. **[low] motion** — src/pages/Landing.tsx:144 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/pages/Landing.tsx:144

6. **[high] mobile-responsive** — UpgradePitchInline.tsx uses flex-wrap without responsive breakpoints. On mobile viewports <375px, two buttons side-by-side may cause horizontal overflow or cramped touch targets.
   Fix: Add responsive prefixes: 'flex flex-col sm:flex-row gap-2' to stack buttons vertically on mobile, horizontal on sm+
   Where: src/components/landing/UpgradePitchInline.tsx:18

7. **[high] mobile-responsive** — UpgradeToVerifiedInline.tsx identical issue: flex-wrap without breakpoints for single-button layout on narrow screens.
   Fix: Add responsive prefixes: 'flex flex-col sm:flex-row gap-2'
   Where: src/components/landing/UpgradeToVerifiedInline.tsx:18

8. **[medium] motion** — Landing.tsx:111 Link with hover:text-emerald-600 lacks transition-colors, causing abrupt color snap (static check confirmed).
   Fix: Add 'transition-colors duration-200' to the className
   Where: src/pages/Landing.tsx:111

9. **[medium] motion** — Landing.tsx:132 and :144 hover states without transitions (static check flagged both).
   Fix: Add 'transition-colors duration-200' or 'transition-all duration-200' to interactive elements
   Where: src/pages/Landing.tsx:132

10. **[medium] design-tokens** — ChatConversation.tsx:210 uses custom CSS property [animation-delay:120ms] and [animation-delay:240ms] instead of Tailwind token pattern.
   Fix: Replace inline delay styles with Tailwind delay utilities (delay-75, delay-150) or use animate-pulse with staggered DOM structure
   Where: src/components/landing/ChatConversation.tsx:210

11. **[medium] accessibility** — ChatConversation.tsx:204-213 thinking indicator uses animate-pulse dots without aria-live or sr-only text for screen readers. Visually impaired users won't know the assistant is processing.
   Fix: Add <span className='sr-only' aria-live='polite'>CropsIntel is thinking</span> or add aria-label to the container div
   Where: src/components/landing/ChatConversation.tsx:204

12. **[low] states** — StarterChips.tsx:39 buttons have hover:bg and hover:border but no focus-visible ring defined. Keyboard users won't see clear focus indication beyond browser defaults.
   Fix: Add 'focus-visible:ring-2 focus-visible:ring-emerald-600/50 focus-visible:ring-offset-2' to button className
   Where: src/components/landing/StarterChips.tsx:39

13. **[low] spacing** — ChatConversation.tsx:221 uses px-4 py-3 for bubbles; design system specifies p-4 (compact) or p-6 (default) for card-like elements. Inconsistent with Card component padding.
   Fix: Change to 'p-4' for consistency with design system compact card padding, or use p-3 if intentionally smaller
   Where: src/components/landing/ChatConversation.tsx:221

## Acceptance criteria

1. All gaps above resolved or explicitly waived in the commit message.
2. `npm run build` clean.
3. Re-run Designer audit — verdict must be `pass`.
