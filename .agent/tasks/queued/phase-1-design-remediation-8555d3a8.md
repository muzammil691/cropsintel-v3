# Task: Design remediation — 8555d3a8

**Auto-generated by Atlas conductor** after Designer audit failed on commit 8555d3a83e344ccc4b311eb628c8cd407a447fe2.

**Original commit:** feat(workshop): approve/reject UI for proposed plan diffs

## Gaps to fix

1. **[low] motion** — src/components/atlas-plan/PlanDiffPreview.tsx:313 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas-plan/PlanDiffPreview.tsx:313

2. **[high] design-tokens** — Approve action buttons use `bg-amber-700 hover:bg-amber-800` (lines 269, 313) instead of primary emerald tokens. Per design system: amber-500 is for warnings only; approval is a primary action and should use `bg-emerald-600 hover:bg-emerald-700`.
   Fix: Replace `className="bg-amber-700 hover:bg-amber-800 text-white"` with `className="bg-emerald-600 hover:bg-emerald-700 text-white transition-colors duration-200"` on both the main approve button and the dialog confirm button.
   Where: src/components/atlas-plan/PlanDiffPreview.tsx:269

3. **[low] motion** — Main approve button at line 269 has `hover:bg-amber-800` but missing explicit `transition-colors duration-200` class (already flagged by static checks for line 313, but applies to 269 as well).
   Fix: Add `transition-colors duration-200` to the approve button className at line 269.
   Where: src/components/atlas-plan/PlanDiffPreview.tsx:269

4. **[high] design-tokens** — Dialog confirm button at line 313 uses amber semantic color for a primary approval action. This creates semantic confusion (amber = warning in the design system).
   Fix: Change `className="bg-amber-700 hover:bg-amber-800 text-white"` to `className="bg-emerald-600 hover:bg-emerald-700 text-white transition-colors duration-200"`
   Where: src/components/atlas-plan/PlanDiffPreview.tsx:313

## Acceptance criteria

1. All gaps above resolved or explicitly waived in the commit message.
2. `npm run build` clean.
3. Re-run Designer audit — verdict must be `pass`.
