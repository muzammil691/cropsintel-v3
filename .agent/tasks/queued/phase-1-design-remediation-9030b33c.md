# Task: Design remediation — 9030b33c

**Auto-generated by Atlas conductor** after Designer audit failed on commit 9030b33c5e5aee251c0a139a4d94592b1c820a7b.

**Original commit:** feat: phase-1.10aj-plan-cockpit-and-wizard (autonomous agent, 960s, 17 files)

## Gaps to fix

1. **[medium] mobile-responsive** — src/components/atlas-plan/PlanActionButtons.tsx: uses shadcn components but has no responsive prefixes (sm:/md:/lg:)
   Fix: Add Tailwind responsive prefixes for layouts that should adapt across viewports
   Where: src/components/atlas-plan/PlanActionButtons.tsx

2. **[medium] mobile-responsive** — src/components/atlas-plan/PlanTree.tsx: uses shadcn components but has no responsive prefixes (sm:/md:/lg:)
   Fix: Add Tailwind responsive prefixes for layouts that should adapt across viewports
   Where: src/components/atlas-plan/PlanTree.tsx

3. **[low] motion** — src/components/atlas-plan/PlanTree.tsx:168 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas-plan/PlanTree.tsx:168

4. **[high] accessibility** — PhaseApprovalBanner buttons use icon-only content without aria-label. Lines 53-79 render <CheckCircle2/>, <Pencil/>, <SkipForward/>, <Pause/> as sole button content, violating WCAG 2.1.1 (keyboard) and 4.1.2 (name). Screen readers cannot announce button purpose.
   Fix: Add aria-label to each Button: aria-label="Approve phase" aria-label="Modify phase" aria-label="Skip phase" aria-label="Pause phase"
   Where: src/components/atlas-plan/PhaseApprovalBanner.tsx:53

5. **[high] accessibility** — ConceptsPanel intake mode buttons (lines ~22-30 based on imports) likely render icon-only buttons for Paperclip/Mic/Plus without text or aria-label. Cannot verify exact line due to truncation but pattern matches PhaseApprovalBanner.
   Fix: Add aria-label="Upload file" aria-label="Record voice" etc to all icon buttons in intake UI
   Where: src/components/atlas-plan/ConceptsPanel.tsx

6. **[high] accessibility** — ConceptsPanel file upload uses imperative fileInputRef.current?.click() pattern (line 17 ref declaration) which bypasses native keyboard flow. Hidden file inputs triggered by custom buttons must have proper focus management.
   Fix: Ensure the trigger button has aria-label and the hidden input has aria-hidden="true". Add onKeyDown handler for Enter/Space on trigger button.
   Where: src/components/atlas-plan/ConceptsPanel.tsx:17

7. **[medium] accessibility** — PhaseWizard Dialog likely missing focus trap verification. DialogContent from shadcn should handle this, but the 'committed' stage (line ~50 committedSummary state) may render custom success UI that bypasses Dialog's focus management.
   Fix: Verify DialogContent wraps all stages including 'committed'. If custom overlay is used, add focus trap via radix-ui/react-focus-scope or keep content inside DialogContent.
   Where: src/components/atlas-plan/PhaseWizard.tsx:50

8. **[medium] mobile-responsive** — PlanActionButtons renders 4 icon buttons in a row with gap-0.5 (line 32) but has no responsive stacking for narrow viewports. On 320px screens, 4 icon buttons + padding may overflow or create cramped 44px touch targets.
   Fix: Add responsive classes: 'flex-wrap sm:flex-nowrap' or 'grid grid-cols-2 sm:flex' to ensure touch targets remain >= 44px on mobile
   Where: src/components/atlas-plan/PlanActionButtons.tsx:32

9. **[medium] mobile-responsive** — PlanTree (truncated at line ~168 based on context) was flagged by static checker as having no responsive prefixes. Likely uses fixed-width columns or indentation that breaks on narrow screens.
   Fix: Add sm:/md:/lg: prefixes for tree indentation (pl-4 sm:pl-6 md:pl-8) and action button visibility (hidden sm:flex for secondary actions)
   Where: src/components/atlas-plan/PlanTree.tsx:168

10. **[low] motion** — PlanTree line 168 hover: class without transition- was flagged by static checker. Abrupt color/background changes create jarring UX.
   Fix: Add transition-colors duration-200 to the hovered element
   Where: src/components/atlas-plan/PlanTree.tsx:168

11. **[medium] states** — PhaseApprovalBanner buttons show :disabled state (line 56 disabled={busy || decided !== null}) but focus-visible ring is not explicitly reinforced. Default shadcn Button should provide this, but custom className on line 58 may override.
   Fix: Verify focus-visible:ring-2 focus-visible:ring-emerald-600/50 is not stripped by custom className. Add explicitly if needed.
   Where: src/components/atlas-plan/PhaseApprovalBanner.tsx:58

12. **[low] typography** — PhaseApprovalBanner uses text-[11px] (lines 43, 54, 63, 66, 74, 77) which is not in the design system scale (text-xs=12px is minimum). Falls below WCAG AA minimum readable size on some displays.
   Fix: Replace text-[11px] with text-xs throughout PhaseApprovalBanner
   Where: src/components/atlas-plan/PhaseApprovalBanner.tsx:43

13. **[low] spacing** — PlanActionButtons uses gap-0.5 (line 32) which is 2px — not in the 4-base scale (gap-1=4px, gap-2=8px). Creates inconsistent micro-spacing.
   Fix: Change gap-0.5 to gap-1 for 4px spacing (still compact, on-scale)
   Where: src/components/atlas-plan/PlanActionButtons.tsx:32

## Acceptance criteria

1. All gaps above resolved or explicitly waived in the commit message.
2. `npm run build` clean.
3. Re-run Designer audit — verdict must be `pass`.
