# Task: Design remediation — eceef830

**Auto-generated by Atlas conductor** after Designer audit failed on commit eceef83096adcdea49e6be1f6e3d57d47e95945c.

**Original commit:** feat: dashboard live state truth (phase-1.10af)

## Gaps to fix

1. **[high] loading-states** — QueueRow.tsx uses raw emoji characters ('⏳' for in-flight, '⏸' for paused) as state indicators instead of proper icon components or semantic loading states. Line ~133: `{isInFlight ? '⏳' : paused ? '⏸' : circleNumber(position ?? 0)}`
   Fix: Replace emoji with lucide-react icons: <Clock className="h-4 w-4" /> for in-flight, <Pause className="h-4 w-4" /> for paused. Wrap in proper semantic container with aria-label.
   Where: src/components/atlas/queue/QueueRow.tsx:133

2. **[high] shadcn-usage** — AtlasQueueTab.tsx implements toast notifications via raw state management (toastMsg state + setTimeout + conditional render) instead of using shadcn/ui Toast or Sonner component. Lines ~28-29 define toastMsg state, ~60-63 showToast function. No accessible toast component imported.
   Fix: Import and use shadcn Toast/Sonner: `import { toast } from 'sonner'` then replace showToast with `toast.success(msg)`. Remove toastMsg state and manual rendering logic. Ensure <Toaster /> is mounted in layout.
   Where: src/components/atlas/tabs/AtlasQueueTab.tsx:28

3. **[medium] accessibility** — QueueRow state emoji ('⏳', '⏸') wrapped only in <span aria-hidden> without corresponding accessible text. Screen readers will miss critical state information that sighted users get from the emoji. Line ~133.
   Fix: Add visually-hidden text or aria-label to parent container: `<div className="sr-only">In progress</div>` alongside the icon, or move aria-label to parent div with role="status".
   Where: src/components/atlas/queue/QueueRow.tsx:133

## Acceptance criteria

1. All gaps above resolved or explicitly waived in the commit message.
2. `npm run build` clean.
3. Re-run Designer audit — verdict must be `pass`.
