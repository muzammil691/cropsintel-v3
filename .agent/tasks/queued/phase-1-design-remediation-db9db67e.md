# Task: Design remediation — db9db67e

**Auto-generated by Atlas conductor** after Designer audit failed on commit db9db67e4da7f05ee823e9ae899ea24e95c9d963.

**Original commit:** feat(workshop): UI tab + plan diff preview + delete old wizard frontend

## Gaps to fix

1. **[medium] mobile-responsive** — src/components/atlas-plan/DecisionLogPanel.tsx: uses shadcn components but has no responsive prefixes (sm:/md:/lg:)
   Fix: Add Tailwind responsive prefixes for layouts that should adapt across viewports
   Where: src/components/atlas-plan/DecisionLogPanel.tsx

2. **[medium] mobile-responsive** — src/components/atlas-plan/PlanDiffPreview.tsx: uses shadcn components but has no responsive prefixes (sm:/md:/lg:)
   Fix: Add Tailwind responsive prefixes for layouts that should adapt across viewports
   Where: src/components/atlas-plan/PlanDiffPreview.tsx

3. **[medium] mobile-responsive** — src/components/atlas-plan/WorkshopSessionList.tsx: uses shadcn components but has no responsive prefixes (sm:/md:/lg:)
   Fix: Add Tailwind responsive prefixes for layouts that should adapt across viewports
   Where: src/components/atlas-plan/WorkshopSessionList.tsx

4. **[low] motion** — src/components/atlas-plan/PlanDiffPreview.tsx:253 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas-plan/PlanDiffPreview.tsx:253

5. **[low] motion** — src/components/atlas-plan/PlanWorkshop.tsx:146 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas-plan/PlanWorkshop.tsx:146

6. **[low] motion** — src/components/atlas-plan/PlanWorkshop.tsx:374 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas-plan/PlanWorkshop.tsx:374

7. **[low] motion** — src/components/atlas-plan/PlanWorkshop.tsx:606 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas-plan/PlanWorkshop.tsx:606

8. **[low] motion** — src/components/atlas-plan/PlanWorkshop.tsx:629 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas-plan/PlanWorkshop.tsx:629

9. **[low] motion** — src/components/atlas-plan/PlanWorkshop.tsx:743 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas-plan/PlanWorkshop.tsx:743

10. **[low] motion** — src/components/atlas-plan/WorkshopSessionList.tsx:110 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas-plan/WorkshopSessionList.tsx:110

11. **[low] motion** — src/components/atlas/tabs/AtlasPlanTab.tsx:461 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas/tabs/AtlasPlanTab.tsx:461

12. **[high] accessibility** — DecisionLogPanel.tsx:227 malformed HTML — <p> tag contains unclosed <sp element (truncated in diff). This will break the DOM tree and cause unpredictable rendering/accessibility failures.
   Fix: Complete the <p> element content and ensure all tags are properly closed. Likely should be <span> based on context.
   Where: src/components/atlas-plan/DecisionLogPanel.tsx:227

13. **[high] mobile-responsive** — DecisionLogPanel.tsx has no responsive prefixes. The component uses fixed text-[11px] and gap-2 spacing that will cause cramped layouts on mobile. Header with Button + title will overflow on narrow viewports.
   Fix: Add sm:/md: breakpoints: header should stack vertically on mobile (flex-col sm:flex-row), text sizes should scale (text-[10px] sm:text-[11px]), and padding should reduce (px-2 sm:px-3).
   Where: src/components/atlas-plan/DecisionLogPanel.tsx

14. **[high] mobile-responsive** — PlanDiffPreview.tsx has no responsive prefixes. The header layout (line 169-179) and action bar will overflow on 375px viewports. Fixed px-4 padding and inline flex layouts will break.
   Fix: Add responsive breakpoints: header padding px-2 sm:px-4, action bar should stack buttons vertically on mobile (flex-col sm:flex-row gap-2), and text sizes should scale.
   Where: src/components/atlas-plan/PlanDiffPreview.tsx

15. **[high] mobile-responsive** — WorkshopSessionList.tsx has no responsive prefixes despite being a sidebar that should collapse on mobile. Fixed grid-cols in PlanWorkshop.tsx line 116 hides this with 'hidden md:flex' but the component itself is not mobile-ready for when it IS shown.
   Fix: Add mobile-first styles: reduce padding (px-2 sm:px-3), smaller text (text-[10px] sm:text-[11px]), and ensure session cards stack properly with min-h-[44px] touch targets.
   Where: src/components/atlas-plan/WorkshopSessionList.tsx

16. **[medium] motion** — PlanDiffPreview.tsx:253 hover state without transition- class causes abrupt visual jump. Static check flagged this correctly.
   Fix: Add transition-colors duration-200 to the element with hover: class.
   Where: src/components/atlas-plan/PlanDiffPreview.tsx:253

17. **[medium] motion** — PlanWorkshop.tsx lines 146, 374, 606, 629, 743 all have hover: classes without transition- causing abrupt state changes. Static checks correctly flagged these.
   Fix: Add transition-colors duration-200 to each interactive element with hover states.
   Where: src/components/atlas-plan/PlanWorkshop.tsx:146

18. **[medium] motion** — WorkshopSessionList.tsx:110 hover state without transition- class.
   Fix: Add transition-colors duration-200 to the hovered element.
   Where: src/components/atlas-plan/WorkshopSessionList.tsx:110

19. **[medium] motion** — AtlasPlanTab.tsx:461 hover state without transition- class.
   Fix: Add transition-colors duration-200 to the hovered element.
   Where: src/components/atlas/tabs/AtlasPlanTab.tsx:461

20. **[medium] accessibility** — PlanDiffPreview.tsx uses AlertTriangle icon (line 8) but the icon appears in truncated diff — cannot verify if it has aria-hidden or descriptive aria-label where used.
   Fix: Ensure all icon instances have aria-hidden="true" if decorative, or aria-label if conveying unique meaning.
   Where: src/components/atlas-plan/PlanDiffPreview.tsx:8

21. **[low] states** — DecisionLogPanel.tsx Pause button (line 60) has focus-visible:ring-2 focus-visible:ring-emerald-600/50 but Button component from shadcn/ui already provides focus styles — this may create double rings.
   Fix: Remove custom focus-visible classes from shadcn Button components unless intentionally overriding. Let the component's built-in focus styles handle it.
   Where: src/components/atlas-plan/DecisionLogPanel.tsx:60

## Acceptance criteria

1. All gaps above resolved or explicitly waived in the commit message.
2. `npm run build` clean.
3. Re-run Designer audit — verdict must be `pass`.
