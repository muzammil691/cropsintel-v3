# Task: Design remediation — cf16b88d

**Auto-generated by Atlas conductor** after Designer audit failed on commit cf16b88d69dc3dc4664c8883f90d315246c346bd.

**Original commit:** feat(workshop): persistent chat input + clickable option pills (Phase A)

## Gaps to fix

1. **[high] design-tokens** — Send button uses `bg-amber-700 hover:bg-amber-800` instead of semantic tokens. Design system specifies amber-500 for warn semantic color, not amber-700/800.
   Fix: Replace with `bg-emerald-600 hover:bg-emerald-700` (primary action) or `bg-amber-500 hover:bg-amber-600` (if warn semantic is intended). Remove `text-white` as it's redundant with button defaults.
   Where: src/components/atlas-plan/PlanWorkshop.tsx:1178

2. **[high] design-tokens** — Generate plan diff button previously used same `bg-amber-700 hover:bg-amber-800` non-standard color (visible in removed lines 1193-1194 context). Should use semantic tokens.
   Fix: Ensure the finalize button uses design-system tokens: either `variant='default'` (emerald primary) or a semantic class like `bg-amber-500 hover:bg-amber-600` if warning color is architecturally required.
   Where: src/components/atlas-plan/PlanWorkshop.tsx:1162

3. **[medium] accessibility** — The Textarea placeholder at line 1144 contains 46+ characters ('Waiting for the next question — you can pre-type or pause.'), which exceeds mobile-friendly placeholder length and may be truncated on narrow viewports (375px target).
   Fix: Shorten placeholder to 'Pre-type your response…' or 'Type ahead (waiting for next question)' — keep under 30 characters for mobile.
   Where: src/components/atlas-plan/PlanWorkshop.tsx:1143

4. **[low] states** — Send button has `title` attribute only when disabled (line 1177), but no aria-label or sr-only text to announce loading state when submitting. Screen readers will only hear 'Send button' during spinner animation.
   Fix: Add `aria-label={submitting ? 'Sending answer…' : 'Send answer'}` to provide dynamic state feedback for assistive tech.
   Where: src/components/atlas-plan/PlanWorkshop.tsx:1174

## Acceptance criteria

1. All gaps above resolved or explicitly waived in the commit message.
2. `npm run build` clean.
3. Re-run Designer audit — verdict must be `pass`.
