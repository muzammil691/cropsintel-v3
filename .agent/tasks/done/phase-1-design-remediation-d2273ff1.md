# Task: Design remediation — d2273ff1

**Auto-generated by Atlas conductor** after Designer audit failed on commit d2273ff16f40a3824db22d2777db3d03fb08f9ae.

**Original commit:** feat: phase-1-design-remediation-cf16b88d (autonomous agent, 100s, 1 files)

## Gaps to fix

1. **[high] typography** — Uses non-standard text sizes `text-[10px]` and `text-[11px]` in header. Design system mandates text-xs (0.75rem / 12px) as the minimum approved size.
   Fix: Replace `text-[10px]` with `text-xs` and `text-[11px]` with `text-xs` throughout the header section. If visual hierarchy is needed between these elements, use font-weight (medium/semibold) or color differentiation instead.
   Where: src/components/atlas-plan/PlanWorkshop.tsx:111

2. **[high] states** — No visible focus-visible styles on interactive elements. The design system requires `:focus-visible (ring-2 ring-emerald-600/50)` on all interactive elements, but none are declared in the component or through shadcn Button variants.
   Fix: Ensure all Button components include focus-visible:ring-2 focus-visible:ring-emerald-600/50 focus-visible:ring-offset-2. If using shadcn Button, verify the variant includes these styles or add them explicitly.
   Where: src/components/atlas-plan/PlanWorkshop.tsx

3. **[medium] accessibility** — Icons (Sparkles, Send, Loader2, etc.) imported from lucide-react are used with `aria-hidden` on line 107, but it's unclear if all decorative icons throughout the component consistently use aria-hidden or if interactive icon buttons have proper aria-label attributes.
   Fix: Audit all lucide-react icon usage: decorative icons must have aria-hidden="true", interactive icon-only buttons must have aria-label. Example: `<Button aria-label="Start new session"><Sparkles /></Button>`
   Where: src/components/atlas-plan/PlanWorkshop.tsx:107

4. **[low] spacing** — Header uses `py-2.5` which is outside the approved 4-base scale (p-1/2/3/4/6/8/12/16). While 2.5 exists in Tailwind, the design system mandates consistency with the specified scale.
   Fix: Replace `py-2.5` with `py-3` (0.75rem) to align with the approved spacing scale.
   Where: src/components/atlas-plan/PlanWorkshop.tsx:100

## Acceptance criteria

1. All gaps above resolved or explicitly waived in the commit message.
2. `npm run build` clean.
3. Re-run Designer audit — verdict must be `pass`.
