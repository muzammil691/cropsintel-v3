# Task: Design remediation — 47bc090f

**Auto-generated by Atlas conductor** after Designer audit failed on commit 47bc090f6bfafcf8f167a0a9964e99a2a1da0cac.

**Original commit:** feat(workshop): explicit Queue button + Archive + priority numbering (1.10bd Step 4)

## Gaps to fix

1. **[low] motion** — src/components/atlas-plan/WorkshopSessionList.tsx:407 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas-plan/WorkshopSessionList.tsx:407

2. **[high] accessibility** — PlanDiffPreview.tsx line ~140-160 (inferred from component structure): expandable detail row toggle uses a raw clickable element without aria-label. All buttons must have text content OR aria-label per design system.
   Fix: Wrap the toggle in shadcn Button component with variant='ghost' and add aria-label='Toggle operation details' or similar descriptive text.
   Where: src/components/atlas-plan/PlanDiffPreview.tsx:150

3. **[medium] motion** — WorkshopSessionList.tsx line 407: hover class without transition- prefix. Static check already flagged 'hover: class without transition- — abrupt state change'. Design system mandates transition-colors duration-200 on all interactive elements.
   Fix: Add 'transition-colors duration-200' to the element's className. If using hover:scale-[1.02], also add 'transition-transform duration-200'.
   Where: src/components/atlas-plan/WorkshopSessionList.tsx:407

4. **[high] states** — Both UI files: interactive card elements and buttons lack explicit focus-visible declarations. Design system requires ':focus-visible (ring-2 ring-emerald-600/50)' on all interactives for keyboard navigation accessibility.
   Fix: Add 'focus-visible:ring-2 focus-visible:ring-emerald-600/50 focus-visible:outline-none' to all Button components and clickable card wrappers (session cards in WorkshopSessionList, operation rows in PlanDiffPreview).
   Where: src/components/atlas-plan/WorkshopSessionList.tsx

5. **[medium] shadcn-usage** — PlanDiffPreview.tsx: operation detail expansion likely uses a raw <div onClick=...> or <button> instead of shadcn Button. Design system anti-pattern: 'Raw <div onClick=...> (use Button or shadcn equivalent)'.
   Fix: Replace raw clickable element with <Button variant='ghost' size='sm'> and proper aria attributes.
   Where: src/components/atlas-plan/PlanDiffPreview.tsx

6. **[medium] accessibility** — WorkshopSessionList.tsx: Archive/Unarchive/Queue action buttons in confirmation dialogs may lack proper disabled states during async operations. Buttons must have :disabled (opacity-50 cursor-not-allowed) per design system.
   Fix: Ensure all action buttons pass disabled={busy !== null} and include 'disabled:opacity-50 disabled:cursor-not-allowed' classes.
   Where: src/components/atlas-plan/WorkshopSessionList.tsx

## Acceptance criteria

1. All gaps above resolved or explicitly waived in the commit message.
2. `npm run build` clean.
3. Re-run Designer audit — verdict must be `pass`.
