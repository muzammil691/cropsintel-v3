# Task: Design remediation — 7b4fbe0a

**Auto-generated by Atlas conductor** after Designer audit failed on commit 7b4fbe0ae4df8ec3495e1b28ee22fbb07b4f3260.

**Original commit:** feat: cockpit v1.2 idea file (phase-1.10al)

## Gaps to fix

1. **[high] typography** — DialogDescription uses `text-[11px]` and footer uses `text-[10px]`, both outside the approved scale (text-xs / text-sm / text-base / text-lg / text-xl / text-2xl / text-3xl only)
   Fix: Change text-[11px] to text-xs (12px) and text-[10px] to text-xs. Update line 85 and 113.
   Where: src/components/atlas-plan/IdeaFileDrawer.tsx:85

2. **[high] loading-states** — Loading state at lines 93-98 uses Loader2 spinner with text 'Loading vision…' instead of required Skeleton component. Design system mandates 'REQUIRED — no spinners alone'.
   Fix: Replace the loading div with proper Skeleton components that match the expected content layout (e.g., multiple Skeleton lines mimicking paragraphs and headings)
   Where: src/components/atlas-plan/IdeaFileDrawer.tsx:93

3. **[high] accessibility** — The `dangerouslySetInnerHTML` at line 108 with custom markdown parser `renderIdeaMarkdown` presents XSS risk and bypasses proper semantic HTML. No sanitization visible in the truncated code.
   Fix: Use a proper markdown library with built-in sanitization (react-markdown with rehype-sanitize) or ensure renderIdeaMarkdown implements DOMPurify or equivalent sanitization
   Where: src/components/atlas-plan/IdeaFileDrawer.tsx:108

4. **[medium] typography** — Icon uses `size-4` utility which is not a standard Tailwind class (should be `w-4 h-4`). While functionally equivalent if defined, it's inconsistent with standard Tailwind naming.
   Fix: Replace `size-4` with `w-4 h-4` on lines 78 and 95 for consistency with standard Tailwind utilities
   Where: src/components/atlas-plan/IdeaFileDrawer.tsx:78

5. **[medium] spacing** — The code element at line 85 uses `px-1` padding which is approved, but the rounded corners lack explicit sizing (should be `rounded` or `rounded-sm` for clarity)
   Fix: Add explicit `rounded-sm` to the code element for consistency: `className="px-1 rounded-sm bg-slate-100..."`
   Where: src/components/atlas-plan/IdeaFileDrawer.tsx:85

6. **[low] typography** — DialogTitle uses `text-base` which is correct, but the icon and text alignment uses gap-2 (compact) when the design system suggests gap-4 (default) for general spacing
   Fix: Consider changing gap-2 to gap-4 on line 78 unless the compact spacing is intentional for the header
   Where: src/components/atlas-plan/IdeaFileDrawer.tsx:78

## Acceptance criteria

1. All gaps above resolved or explicitly waived in the commit message.
2. `npm run build` clean.
3. Re-run Designer audit — verdict must be `pass`.
