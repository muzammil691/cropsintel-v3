# Task: Design remediation — beb1288b

**Auto-generated by Atlas conductor** after Designer audit failed on commit beb1288beacc63ba29c0b0c723ff86f7e6c2a6bc.

**Original commit:** feat: phase-1-design-remediation-0520947b

## Gaps to fix

1. **[medium] mobile-responsive** — src/components/ui/select.tsx: uses shadcn components but has no responsive prefixes (sm:/md:/lg:)
   Fix: Add Tailwind responsive prefixes for layouts that should adapt across viewports
   Where: src/components/ui/select.tsx

2. **[high] shadcn-usage** — dropdown-menu.tsx line 2: incorrect import 'radix-ui' instead of '@radix-ui/react-dropdown-menu' - component will fail at runtime
   Fix: Change import to: import * as DropdownMenuPrimitive from '@radix-ui/react-dropdown-menu'
   Where: src/components/ui/dropdown-menu.tsx:2

3. **[medium] design-tokens** — dropdown-menu.tsx line 195 and select.tsx line 349: uses 'text-popover-foreground' which is not in the design system color tokens (should use text-slate-700 dark:text-slate-300)
   Fix: Replace 'text-popover-foreground' with 'text-slate-700 dark:text-slate-300' to match design tokens
   Where: src/components/ui/dropdown-menu.tsx:195

4. **[medium] shadcn-usage** — dropdown-menu.tsx line 14-19: DropdownMenuTrigger does not accept asChild prop, breaking typical shadcn composition pattern where trigger can wrap custom elements
   Fix: Add asChild prop support: function DropdownMenuTrigger({ asChild, ...props }: React.ComponentProps<typeof DropdownMenuPrimitive.Trigger> & { asChild?: boolean })
   Where: src/components/ui/dropdown-menu.tsx:14

5. **[medium] accessibility** — ConnectionCard.tsx line 186: DropdownMenuTrigger uses className for focus-visible but missing outline-none, may cause double focus ring
   Fix: The component already has 'outline-none' in className - this is correct. Mark as resolved.
   Where: src/components/atlas/ConnectionCard.tsx:186

6. **[low] states** — dropdown-menu.tsx line 219-220: focus-visible:ring-2 is applied but the component also has focus:bg-slate-50, which may cause confusion between keyboard and mouse focus
   Fix: This is acceptable as both states can coexist (background for all focus, ring for keyboard). Consider removing this gap.
   Where: src/components/ui/dropdown-menu.tsx:219

## Acceptance criteria

1. All gaps above resolved or explicitly waived in the commit message.
2. `npm run build` clean.
3. Re-run Designer audit — verdict must be `pass`.
