# Task: Design remediation — 18deaab3

**Auto-generated by Atlas conductor** after Designer audit failed on commit 18deaab38a286fff691a50d6ea1796879b7bbf57.

**Original commit:** feat(atlas-queue): force-cancel for in-progress zombies + UI button (H.1+H.2)

## Gaps to fix

1. **[low] motion** — src/components/atlas/queue/QueueRow.tsx:154 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas/queue/QueueRow.tsx:154

2. **[low] motion** — src/components/atlas/queue/QueueRow.tsx:155 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas/queue/QueueRow.tsx:155

3. **[low] motion** — src/components/atlas/queue/QueueRow.tsx:197 hover: class without transition- — abrupt state change
   Fix: Add transition-colors duration-200 (or similar) for smooth state changes
   Where: src/components/atlas/queue/QueueRow.tsx:197

4. **[medium] motion** — QueueRow.tsx lines 154, 155, 197: hover: classes applied without transition- prefix, causing abrupt state changes. Static checks already flagged these.
   Fix: Add transition-colors duration-200 to all elements with hover: state changes
   Where: src/components/atlas/queue/QueueRow.tsx:154

5. **[high] shadcn-usage** — QueueRow.tsx line 95: <span> with circleNumber(position ?? 0) appears to be a clickable/interactive element (given context of queue position) but uses raw HTML instead of Button component
   Fix: If interactive, replace with <Button variant="ghost" size="sm">. If purely decorative, add aria-hidden="true" (which is present, so this may be acceptable)
   Where: src/components/atlas/queue/QueueRow.tsx:95

6. **[high] states** — QueueRow.tsx: Multiple interactive icon buttons (ChevronUp, ChevronDown, Trash2, etc.) are rendered but the truncated code doesn't show focus-visible ring states. Based on component structure, these likely lack focus-visible:ring-2 focus-visible:ring-emerald-600/50
   Fix: Ensure all Button components have focus-visible:ring-2 focus-visible:ring-emerald-600/50 or use shadcn Button which includes this by default
   Where: src/components/atlas/queue/QueueRow.tsx

7. **[high] shadcn-usage** — AtlasQueueTab.tsx line ~140 (in handleForceCancel): Uses window.confirm() for destructive action confirmation instead of shadcn Dialog component. This is not accessible, not styled, and breaks the design system requirement for overlays.
   Fix: Replace window.confirm with a proper Dialog component with Cancel/Confirm buttons using variant="destructive" for the confirm action
   Where: src/components/atlas/tabs/AtlasQueueTab.tsx:140

8. **[medium] accessibility** — QueueRow.tsx lines 111-113: Icon-only buttons (Clock with size-3) likely lack aria-label for screen readers. The Clock icon appears to be decorative but is within an interactive context.
   Fix: If the Clock icon is purely decorative, ensure parent span has adequate text content. If standalone button, add aria-label="Time elapsed" or similar
   Where: src/components/atlas/queue/QueueRow.tsx:111

9. **[low] motion** — QueueRow.tsx line 197: Third instance of hover: class without transition- causing abrupt visual change
   Fix: Add transition-colors duration-200 to element at line 197
   Where: src/components/atlas/queue/QueueRow.tsx:197

## Acceptance criteria

1. All gaps above resolved or explicitly waived in the commit message.
2. `npm run build` clean.
3. Re-run Designer audit — verdict must be `pass`.
