---
adr: 2026-05-07-verifier-cluster-1778162863683
date: 2026-05-07
status: closed-duplicate
investigators: atlas-conductor, autonomous-agent
trigger: 3 Verifier failures within 30 minutes (cluster id 1778162863683)
duplicate-of: ADR-2026-05-07-verifier-cluster-1778160826442.md
related:
  - ADR-2026-05-07-verifier-cluster-failure.md
  - ADR-2026-05-07-verifier-cluster-1778147114729.md
  - ADR-2026-05-07-verifier-cluster-1778149686008.md
  - ADR-2026-05-07-verifier-cluster-1778152873297.md
  - ADR-2026-05-07-verifier-cluster-1778153395522.md
  - ADR-2026-05-07-verifier-cluster-1778154179464.md
  - ADR-2026-05-07-verifier-cluster-1778160826442.md
  - ADR-2026-05-07-verifier-cluster-1778161030385.md
  - ADR-2026-05-07-verifier-cluster-1778161525828.md
  - ADR-2026-05-07-verifier-cluster-1778162376501.md
---

# ADR — Verifier cluster `1778162863683`: duplicate of `1778160826442`; close, both canonical fixes shipped, no new code change

## Context

Atlas conductor flagged a 3-failure Verifier cluster in a 30-minute window (2-of-3 multi-brain majority → investigate). All three failures are themselves cluster-investigation tasks on the **same lineage** that has been ADR'd nine times already today:

| time (UTC)            | task id                                                       |
|-----------------------|---------------------------------------------------------------|
| 2026-05-07 13:49:11   | `phase-1-CLUSTER-investigation-1778161030385`                 |
| 2026-05-07 13:57:32   | `phase-1-CLUSTER-investigation-1778161030385-rem`             |
| 2026-05-07 14:05:37   | `phase-1-CLUSTER-investigation-1778161030385-rem2`            |

This is the **tenth** duplicate cluster ADR on the 1.10af-quality-gates lineage in <2.5 hours, and exactly the outcome the predecessor ADR (`1778162376501`) predicted at HEAD `4b7c931`:

> "The three remaining queued cluster investigations (`1778162863683`, `1778163111379`, `1778163384092`) all queued before the fix activation timestamps and are expected to be handled identically — close as duplicate, no code change."

This ADR is the first of those three to drain.

## Why this cluster fired despite both canonical fixes being at HEAD

Cluster id encodes queue-time: `1778162863683` ms → 2026-05-07T14:27:43Z. Canonical fix ship times:

| Canonical fix                                          | Ship commit | Ship time (UTC)         |
|--------------------------------------------------------|-------------|-------------------------|
| Conductor cluster-dedupe upgrade (rem3, 4-gate)        | `517e407`   | 2026-05-07T14:52:25Z    |
| Verifier parser subject-matter immunity                | `1bede69`   | 2026-05-07T15:09:45Z    |
| Predecessor cluster close (`1778162376501`)            | `4b7c931`   | 2026-05-07T15:13:42Z    |

This cluster was queued **24 minutes before the dedupe gate activated and 42 minutes before the parser immunity shipped.** It is in-flight residue of the historical failure index, not a new failure mode. None of the three constituent failure records post-date either canonical fix — they all sit at 13:49 / 13:57 / 14:05 UTC, well before the 14:52 dedupe ship.

## Re-verification of root-cause hypotheses at HEAD `4b7c931`

| # | Hypothesis | Verdict | Evidence at HEAD |
|---|---|---|---|
| 1 | Recent migration broke a contract | **Ruled out.** | `git log -5 -- supabase/migrations/` shows last two migrations are `1bede69` (`verifier_subject_matter_hits` audit table — additive) and `8366823` (Adela daily brief — additive). Neither modifies an existing contract. |
| 2 | Env var missing or rotated | **Ruled out.** | No env changes between `7466c3c` (rolling green tip) and `4b7c931` (current HEAD). Same env produced the green tip at `7466c3c` and seven duplicate ADRs since. |
| 3 | Builder picked up a stale base | **Ruled out.** | All 1.10af lineage tasks remain in `.agent/tasks/done/`. Conductor dedupe rem3 and parser immunity both in `done/`. Rolling green tip at `4b7c931`. |
| 4 | Verifier prompt regression | **Ruled out and structurally immune.** | `verifier/src/lib/council-parser.ts:25-48,70-79` masks subject-matter fail-keyword positions inside backtick spans, fenced blocks, short quoted strings, task-id tokens, file paths, and a 40-word window after `subject:`/`topic:`/`investigating`/`diagnose`-class introducers. Migration `20260507120000_verifier_subject_matter_hits.sql` records audit hits. Both shipped in `1bede69`. |

## Root cause

Identical to the nine predecessors — fully remediated upstream. No new code change is in scope (acceptance criterion 2 N/A; criterion 3 applies — false alarm, documented and closed).

## Decision

**Close cluster `1778162863683` as duplicate of `1778160826442`. No new code change.** This cluster is the second of three trailing-residue clusters predicted by ADR `1778162376501`; closing it confirms the prediction model.

## Forward-looking expectation

Two queued clusters remain on the same lineage: `1778163111379` and `1778163384092`. Both encode queue-times before 14:52:25 (`517e407` ship), so both are expected to drain identically — close as duplicate, no code change. After they drain, the dedupe gate should prevent any further queueing on the 1.10af failure record set.

If a cluster fires whose **id timestamp post-dates** `1778163345145000` (= 14:52:25 UTC) AND it cites the 1.10af failure records, the dedupe gate is not actually filtering them and a follow-up investigation under a fresh task ID would be warranted. No current evidence for that.

## Follow-ups

No new follow-ups. Open items unchanged from predecessor ADR:

1. **Conductor cluster-dedupe upgrade** — ✅ shipped (`189c826` → `9287523` → `b42f985` → `517e407`).
2. **Verifier parser subject-matter immunity** — ✅ shipped (`1bede69`).
3. **Cluster spec must carry Verifier gap text** — open, conductor track. Lower priority now that the dedupe gate is live.
4. **Bug B1** (Builder commits "feat: …" with only log-file deltas) — open, agent-loop track.
5. **Bug B2** (judges treat empty/log-only diff as missing implementation) — open, verifier track.
6. **Bug B3 candidate** (judges fail-fast on investigation/ADR-only specs) — partially addressed by `1bede69`; remaining `empty-diff-guard` short-circuit at `verifier/src/verify.ts:88-105` still triggers when `filesRequired` parses empty. Lower priority — ADR specs ship `## Files` sections by convention.

## Files

- `docs/atlas-decisions/ADR-2026-05-07-verifier-cluster-1778162863683.md`
- `.agent/tasks/in-progress/phase-1-CLUSTER-investigation-1778162863683.md`
- `verifier/src/lib/council-parser.ts`
- `supabase/migrations/20260507120000_verifier_subject_matter_hits.sql`

## Status

**Closed — duplicate of `1778160826442`. Both canonical fixes shipped at HEAD `4b7c931`. No production code change.**
