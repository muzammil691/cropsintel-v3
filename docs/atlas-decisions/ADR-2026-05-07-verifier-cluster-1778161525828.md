---
adr: 2026-05-07-verifier-cluster-1778161525828
date: 2026-05-07
status: closed-duplicate
investigators: atlas-conductor, autonomous-agent
trigger: 3 Verifier failures within 30 minutes (cluster id 1778161525828)
duplicate-of: ADR-2026-05-07-verifier-cluster-1778160826442.md
related:
  - ADR-2026-05-07-verifier-cluster-failure.md
  - ADR-2026-05-07-verifier-cluster-1778147114729.md
  - ADR-2026-05-07-verifier-cluster-1778149686008.md
  - ADR-2026-05-07-verifier-cluster-1778152873297.md
  - ADR-2026-05-07-verifier-cluster-1778153395522.md
  - ADR-2026-05-07-verifier-cluster-1778154179464.md
  - ADR-2026-05-07-verifier-cluster-1778160826442.md
  - ADR-2026-05-07-verifier-cluster-1778161030385.md
---

# ADR — Verifier cluster `1778161525828`: duplicate of `1778160826442`; close, no new code action

## Context

Atlas conductor flagged a 3-failure Verifier cluster in a 30-minute window (2-of-3 multi-brain majority → investigate):

| time (UTC)            | task                                                          |
|-----------------------|---------------------------------------------------------------|
| 2026-05-07 13:16:06   | `phase-1.10af-workflow-quality-gates-fix`                     |
| 2026-05-07 13:22:04   | `phase-1.10af-workflow-quality-gates-fix-rem`                 |
| 2026-05-07 13:32:15   | `phase-1.10af-workflow-quality-gates-fix-rem2`                |

These three IDs and timestamps are **bit-identical** to the prior closed clusters `1778160826442` (closed in `93975a8`) and `1778161030385` (closed in `30378ab`). The predecessor ADR explicitly predicted: *"this loop will continue burning agent budget"* until the conductor cluster-dedupe upgrade ships. This is now the **eighth** duplicate cluster ADR on the same `phase-1.10af` lineage.

## Re-verification of hypotheses at HEAD `30378ab`

| # | Hypothesis | Verdict | Evidence at HEAD |
|---|---|---|---|
| 1 | Recent migration broke a contract | **Ruled out.** | `git log -1 -- supabase/migrations/` returns `8366823` (Adela daily brief, 07:49 UTC) — predates all three failures and is unchanged since. |
| 2 | Env var missing or rotated | **Ruled out.** | The remediated lineage shipped `7466c3c` after the failure window using the same env; no auth/env errors in any rem-task log. |
| 3 | Builder picked up a stale base | **Ruled out.** | All four rem-chain tasks (`phase-1.10af-workflow-quality-gates-fix{,-rem,-rem2,-rem3}`) reside in `.agent/tasks/done/`; rem3 (`7466c3c`) remains the rolling green tip. |
| 4 | Verifier prompt regression | **Confirmed and already fixed.** | Verifier self-flagging on stub-pattern regex literals inside its own source files. Fixed by `6d7a626` (SCAN_EXCLUSIONS + relocated stub-detector tests), `de58d26` (source patterns immune to self-flag), and `38c97e5` (e2e-smoke skips when runner deps missing). All three commits remain present at HEAD. |

## Why the conductor fired (yet again)

The conductor's failure index still carries the historical 13:16 / 13:22 / 13:32 records and queued this cluster at `a4a37e1`, **after** both predecessor clusters had been queued and one had already been closed as duplicate (`93975a8`). The conductor cluster-dedupe upgrades carried as URGENT follow-ups in every prior ADR have **still not yet shipped**, so the conductor keeps re-firing on the same three already-resolved IDs.

Cluster ADR count today on this single lineage: **8**.

## Root cause

Identical to predecessor — already remediated upstream:

1. **Verifier self-flagging** — fixed in `6d7a626` and `de58d26`.
2. **e2e-smoke crash on missing runner deps** — fixed in `38c97e5`.

No new code change is in scope for this investigation (per acceptance criterion 2; criterion 3 applies — false alarm, documented and closed).

## Decision

**Close cluster `1778161525828` as duplicate of `1778160826442`, no new code change.** The rem-chain converged on a green build at `7466c3c` before this cluster was even queued.

## Follow-ups

No new follow-ups. The canonical items remain unchanged from prior ADRs:

1. **Conductor cluster-dedupe upgrade (URGENT)** — open, conductor track. Eight duplicate ADRs in <3 hours on this lineage. Until the conductor dedupes against (a) closed ADRs, (b) queued/in-progress clusters within trailing 30 minutes, and (c) failure records whose remediation shipped at HEAD, this loop will keep burning agent budget.
2. **Cluster spec must carry Verifier gap text** — open, conductor track. Every cluster in this lineage records `no detail` per failure, forcing investigators into git archeology.
3. **Bug B1** (Builder commits "feat: …" with only log-file deltas while real changes live in adjacent commits) — open, agent-loop track.
4. **Bug B2** (judges treat empty/log-only diff as missing implementation) — open, verifier track.
5. **Bug B3 candidate** (judges fail-fast on investigation/ADR-only specs) — open, verifier track.

## Files

- `docs/atlas-decisions/ADR-2026-05-07-verifier-cluster-1778161525828.md`

## Status

**Closed — duplicate of `1778160826442`.** No code change.
