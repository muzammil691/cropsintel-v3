---
adr: 2026-05-07-verifier-cluster-1778149686008
date: 2026-05-07
status: closed-no-action
investigators: atlas-conductor, autonomous-agent
trigger: 3 Verifier failures within 30 minutes (cluster id 1778149686008)
supersedes: none
related:
  - ADR-2026-05-07-verifier-cluster-failure.md
  - ADR-2026-05-07-verifier-cluster-1778147114729.md
---

# ADR — Verifier cluster `1778149686008` is a third duplicate trigger; no new action

## Context

Atlas conductor flagged a Verifier-failure cluster of 3 tasks in a 30-minute window:

| time (UTC)              | task                                                   |
|-------------------------|--------------------------------------------------------|
| 2026-05-07 10:23:45     | `phase-1-CLUSTER-investigation-1778147114729`          |
| 2026-05-07 10:20:39     | `phase-1.00f1-verifier-spec-path-after-sync-rem2`      |
| 2026-05-07 10:05:40     | `phase-1.00f1-verifier-spec-path-after-sync-rem`       |

2-of-3 multi-brain majority voted "investigate".

This is the **third** cluster trigger today over substantively the same set of underlying failures. The two prior triggers are already closed in:

- `ADR-2026-05-07-verifier-cluster-failure.md` (root cause analysis — three coincident bugs A/B/C)
- `ADR-2026-05-07-verifier-cluster-1778147114729.md` (first duplicate close — no action)

## Investigation

Each constituent task ID has already been adjudicated and the work has shipped:

| task ID | current state | evidence |
|---|---|---|
| `phase-1-CLUSTER-investigation-1778147114729` | Accepted, in `done/`. ADR shipped at `e949682`. | `git log --oneline -- docs/atlas-decisions/` shows the ADR; spec is in `.agent/tasks/done/`. |
| `phase-1.00f1-verifier-spec-path-after-sync-rem2` | Accepted, in `done/`. Code at `18c62a5`, test fix at `b587640`. | Verifier source last touched at `b587640`; that audit passed. |
| `phase-1.00f1-verifier-spec-path-after-sync-rem` | Superseded by `-rem2` (and ultimately `-rem3`); in `done/`. The Bug A fix it carried (`7a339ce`) is in main and is verified live in `verifier/src/server.ts` (line 115 `syncRepoToHead` runs before line 139 `findTaskSpec`). | Inspected at HEAD `578ee4b`. |

The four hypotheses in the spec are all already closed:

| # | Hypothesis | Verdict | Evidence at HEAD `578ee4b` |
|---|---|---|---|
| 1 | Recent migration broke a contract | **Ruled out** (re-confirmed). | `git log -- supabase/migrations/` shows no new commits since the prior ADRs were written. |
| 2 | Env var missing or rotated | **Ruled out** (re-confirmed). | All three failed task IDs are explained by the prior ADRs' Bug A / empty-diff-guard analysis — none traced to env. |
| 3 | Builder picked up a stale base | **Tracked as Bug B** in the prior ADR (Builder 0-file pattern). Still on the agent-loop track; not a Verifier defect; out of scope here. | `phase-1.00f1-…-rem3` itself committed `0 files` at `578ee4b` and was still accepted — same Bug B family. |
| 4 | Verifier prompt regression | **Ruled out** (re-confirmed). | Last verifier source commit is `b587640` (the rem2 test fix); nothing since. |

## Why this cluster is a duplicate

Atlas's failure-window query keeps catching three older failure log entries that pre-date the cluster window's last accepted commit. None of them is a new defect:

- `phase-1.00f1-…-rem` and `-rem2` are interim attempts at the Bug A fix; the canonical fix shipped at `7a339ce`, the test was tightened at `b587640`, and the loop ultimately accepted `-rem3` at `578ee4b` (`atlas: phase-1.00f1-verifier-spec-path-after-sync-rem3 complete`, `4213076`).
- `phase-1-CLUSTER-investigation-1778147114729` is the prior duplicate-cluster ADR itself, accepted at `e949682`.

The pattern from the first duplicate ADR's operational note is now confirmed twice over:

> To prevent further duplicate cluster triggers on the same underlying failures, the conductor's failure-window query should consider whether the constituent task IDs already appear under an ADR with `status != open`. If yes, dedupe the trigger.

That conductor-side dedupe has not yet shipped; until it does, additional duplicate triggers like this one are expected whenever the failure window slides over older closed entries.

## Decision

**Close cluster `1778149686008` with no new code action.** The cluster is a third duplicate trigger over already-diagnosed failures. No follow-up tasks are spawned by this ADR. The action items from the original root-cause ADR remain canonical:

1. **Bug A — verifier spec path after sync** — shipped (`7a339ce`), test tightened (`b587640`), accepted on `-rem2` and `-rem3`.
2. **Bug B — Builder 0-file pattern** — open, agent-loop track. Visible again on this very chain (`-rem3` accepted with 0 files committed); still not in Verifier scope.
3. **Bug C — Verifier spec-parser `docs/` allowlist** — open, verifier track. Not exercised by this audit because this ADR declares its file via backticks (see Files section below), which the parser already supports.
4. **Migration filename collision** (`atlas_schema_complete.sql` ×2) — open, phase-1.10b2 follow-up.
5. **Conductor cluster dedupe** — open, conductor track. Recommended again here: dedupe a new cluster against the union of task IDs already cited in any closed ADR.

Per acceptance criterion 3, this is documented as no-action and the cluster is closed.

## Files

- `docs/atlas-decisions/ADR-2026-05-07-verifier-cluster-1778149686008.md`

## Status

**Closed — no action.** Third duplicate of the original `ADR-2026-05-07-verifier-cluster-failure.md`. Read that ADR for the full root-cause analysis. The conductor-side dedupe recommendation is escalated; until it ships, expect further duplicates on the same underlying failures.
