---
adr: 2026-05-07-verifier-cluster-1778162376501
date: 2026-05-07
status: closed-duplicate
investigators: atlas-conductor, autonomous-agent
trigger: 3 Verifier failures within 30 minutes (cluster id 1778162376501)
duplicate-of: ADR-2026-05-07-verifier-cluster-1778160826442.md
related:
  - ADR-2026-05-07-verifier-cluster-failure.md
  - ADR-2026-05-07-verifier-cluster-1778147114729.md
  - ADR-2026-05-07-verifier-cluster-1778149686008.md
  - ADR-2026-05-07-verifier-cluster-1778152873297.md
  - ADR-2026-05-07-verifier-cluster-1778153395522.md
  - ADR-2026-05-07-verifier-cluster-1778154179464.md
  - ADR-2026-05-07-verifier-cluster-1778160826442.md
  - ADR-2026-05-07-verifier-cluster-1778161030385.md
  - ADR-2026-05-07-verifier-cluster-1778161525828.md
---

# ADR — Verifier cluster `1778162376501`: duplicate of `1778160826442`; close, both canonical fixes now shipped

## Context

Atlas conductor flagged a 3-failure Verifier cluster in a 30-minute window (2-of-3 multi-brain majority → investigate):

| time (UTC)            | task                                                          |
|-----------------------|---------------------------------------------------------------|
| 2026-05-07 13:32:15   | `phase-1.10af-workflow-quality-gates-fix-rem2`                |
| 2026-05-07 13:49:11   | `phase-1-CLUSTER-investigation-1778161030385`                 |
| 2026-05-07 13:57:32   | `phase-1-CLUSTER-investigation-1778161030385-rem`             |

These three failure records are stale historical artifacts from the same lineage that has been ADR'd eight times already today. The lineage converged on a green build at `7466c3c` (rem3 of the original 1.10af spec) and at `1bede69` (verifier parser immunity); the conductor's failure index simply replays the same 13:32 / 13:49 / 13:57 records in different 30-minute windowings.

This is the **ninth** duplicate cluster ADR on this lineage in <2.5 hours.

## What changed since the predecessor ADR

Both canonical follow-ups called out in every prior ADR have now **shipped at HEAD** (`1bede69`):

1. **Conductor cluster-dedupe upgrade** — `phase-conductor-cluster-dedupe-upgrade{,-rem,-rem2,-rem3}` shipped through `189c826` → `9287523` → `b42f985` → `517e407`. Persistent dedupe gate against (a) closed ADRs, (b) queued/in-progress clusters within trailing 30 minutes, (c) failure records whose remediation has shipped at HEAD.
2. **Verifier parser subject-matter immunity** — `phase-verifier-parser-subject-matter-immunity` shipped at `1bede69`. `verifier/src/lib/council-parser.ts` now masks fail-keyword positions inside backtick spans, fenced blocks, short quoted strings, task-id tokens, file paths, and the 40-word window after a "subject:"/"topic:"/"investigating"/"diagnose"-class introducer. Audit row written to `verifier_subject_matter_hits` table (`supabase/migrations/20260507120000_verifier_subject_matter_hits.sql`).

The reason this cluster fired anyway is **timing**: the conductor queued cluster `1778162376501` at `ec95249` (13:59:36 UTC), but the conductor dedupe rem3 only shipped at `517e407` (14:52:25 UTC) and the parser immunity fix only shipped at `1bede69` (15:09:45 UTC). This cluster was therefore in-flight before either canonical fix activated, and is the trailing residue of the old failure mode rather than a new failure.

## Re-verification of root-cause hypotheses at HEAD `1bede69`

| # | Hypothesis | Verdict | Evidence at HEAD |
|---|---|---|---|
| 1 | Recent migration broke a contract | **Ruled out.** | `git log -1 -- supabase/migrations/` returns `1bede69` (verifier subject-matter audit table), preceded by `8366823` (Adela daily brief). Both add new tables; neither modifies existing contracts. |
| 2 | Env var missing or rotated | **Ruled out.** | No env changes between `7466c3c` (rolling green tip on 1.10af lineage), `530bf9f` (rem1 ship), `00ab384` (rem2 ship), `aa07c5f` (rem3 close), `517e407` (conductor dedupe ship), and `1bede69` (parser immunity ship). All ran on the same env that produced the green tip. |
| 3 | Builder picked up a stale base | **Ruled out.** | All 1.10af lineage tasks remain in `.agent/tasks/done/`. The follow-up specs (`phase-conductor-cluster-dedupe-upgrade*`, `phase-verifier-parser-subject-matter-immunity`) are also in `.agent/tasks/done/`. The rolling green tip is `1bede69`. |
| 4 | Verifier prompt regression | **Ruled out and now structurally immune.** | The original self-flagging stub-pattern issue was fixed in `6d7a626`, `de58d26`, `38c97e5` (still at HEAD). The follow-on subject-matter regex over-trigger that produced `judgment-synthesis-judgment-text-fail` rejections on rem3 is now masked at `verifier/src/lib/council-parser.ts:25-48,70-79` (shipped in `1bede69`). |

The duplicate-cluster verdict from the eight predecessor ADRs stands unchanged.

## Root cause

Identical to predecessors — already fully remediated upstream:

1. **Verifier self-flagging on stub-pattern regex literals** — fixed in `6d7a626`, `de58d26`, `38c97e5`.
2. **e2e-smoke crash on missing runner deps** — fixed in `38c97e5`.
3. **Council parser subject-matter regex over-trigger** — fixed in `1bede69` (this fix is the new development since the previous ADR).

No new code change is in scope for this investigation (per acceptance criterion 2; criterion 3 applies — false alarm, documented and closed).

## Decision

**Close cluster `1778162376501` as duplicate of `1778160826442`, no new code change.** This cluster was queued before both canonical fixes activated; it is the trailing residue of the historical failure index, not a new failure.

## Forward-looking expectation

With both canonical fixes now at HEAD, the conductor should not queue further clusters from this lineage. The three remaining queued cluster investigations (`1778162863683`, `1778163111379`, `1778163384092`) all queued before the fix activation timestamps and are expected to be handled identically — close as duplicate, no code change. After they drain, the persistent dedupe gate from `517e407` should prevent any further queueing on the same failure record set.

If the conductor keeps queueing new clusters from this lineage **after** all four currently-queued investigations drain, the dedupe gate is not actually active for these failure records and a follow-up investigation under a fresh task ID would be warranted. The current evidence does not support that hypothesis.

## Follow-ups

No new follow-ups. Both canonical items have shipped:

1. **Conductor cluster-dedupe upgrade** — ✅ shipped (`189c826` → `9287523` → `b42f985` → `517e407`).
2. **Cluster spec must carry Verifier gap text** — open, conductor track. (Still relevant: every cluster in this lineage records `no detail` per failure, forcing investigators into git archeology. Lower priority now that the dedupe gate is shipped.)
3. **Bug B1** (Builder commits "feat: …" with only log-file deltas while real changes live in adjacent commits) — open, agent-loop track.
4. **Bug B2** (judges treat empty/log-only diff as missing implementation) — open, verifier track.
5. **Bug B3 candidate** (judges fail-fast on investigation/ADR-only specs) — partially addressed by `1bede69` parser immunity; the remaining `empty-diff-guard` short-circuit at `verifier/src/verify.ts:88-105` still triggers on specs whose `filesRequired` parses empty. Lower priority now that ADR specs ship `## Files` sections by convention.

## Files

- `docs/atlas-decisions/ADR-2026-05-07-verifier-cluster-1778162376501.md`
- `.agent/tasks/in-progress/phase-1-CLUSTER-investigation-1778162376501.md`
- `verifier/src/lib/council-parser.ts`
- `supabase/migrations/20260507120000_verifier_subject_matter_hits.sql`

## Status

**Closed — duplicate of `1778160826442`. Both canonical fixes now shipped at HEAD `1bede69`.** No production code change.
