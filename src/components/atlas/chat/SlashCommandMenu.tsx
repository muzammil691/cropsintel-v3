// Co-located re-export of the canonical SlashCommandMenu (1.10an-remediation-2)
//
// The chat input pop-over surfacing the slash-command registry. Implementation
// lives at the sibling path to avoid a parallel implementation; this barrel
// makes it importable from the spec'd `chat/` subdirectory.

export { SlashCommandMenu } from '../SlashCommandMenu'
