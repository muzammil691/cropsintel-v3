// Co-located re-export of the canonical ArtifactCardInChat (1.10an-remediation-2)
//
// Inline tool-result card with success / error states + Diagnose / Copy Fix /
// Retry actions. Implementation lives at the sibling path; this barrel makes
// it importable from the spec'd `chat/` subdirectory.

export { ArtifactCardInChat } from '../ArtifactCardInChat'
